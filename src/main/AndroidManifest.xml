<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.itwpro.diet_trackerboom">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application android:name=".models.MyApplication" android:allowBackup="true" android:label="@string/app_name" android:roundIcon="@mipmap/ic_launcher_round" android:icon="@mipmap/ic_launcher" android:supportsRtl="true" android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <!--  Entry point  -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".SignupActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".ProfileSetupActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".HomeActivity"/>
        <activity android:name=".AddFoodActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".MealHistoryActivity"/>
        <activity android:name=".ProgressActivity"/>
    </application>
</manifest>